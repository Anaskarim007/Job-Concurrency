# Next.js Landing Page — Workflow Test

A small Next.js static landing page designed to test a GitHub Actions workflow.

## Commands

```bash
npm install
npm run build
```

The project uses `output: "export"` so the production build creates an `out/` folder.
